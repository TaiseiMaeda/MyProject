#読み込み時、もともとの行数とmerge後の行数。馬場ごとに行う。
length(Tanhuku2hunBaba [,1]);length(TanNinki2hunBaba [,1]);length(RaceJohoKekkaBaba [,1]);length(Umahuku2hunBaba [,1]);
#結合後
length(merge1[,1]);length(merge2[,1]);length(UmahukuMat[,1])




